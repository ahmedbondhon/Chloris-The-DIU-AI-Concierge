# 🌿 Chloris AI Concierge: Step-by-Step Setup Guide (Bengali)

এই প্রোজেক্টটি Daffodil International University (DIU)-এর জন্য তৈরি একটি AI Concierge সিস্টেম। নিচে এটি শুরু থেকে শেষ পর্যন্ত রান করার নিয়মগুলো দেওয়া হলো।

---

## 🛠️ প্রয়োজনীয় সফটওয়্যার (Prerequisites)

প্রোজেক্টটি রান করার আগে নিশ্চিত করুন আপনার পিসিতে নিচের সফটওয়্যারগুলো আছে:

1. **Node.js** (v18 বা তার বেশি)
2. **Python** (3.10 বা তার বেশি)

---

## 1️⃣ ব্যাকএন্ড সেটআপ (FastAPI Backend)

ব্যাকএন্ড রান করার জন্য নিচের কমান্ডগুলো সিরিয়াল অনুযায়ী দিন:

1. **ব্যাকএন্ড ফোল্ডারে যান:**

   ```bash
   cd BackEnd/app
   ```

2. **পাইথন ভার্চুয়াল এনভায়রনমেন্ট তৈরি করুন:**

   ```bash
   python -m venv venv
   ```

3. **ভার্চুয়াল এনভায়রনমেন্ট অ্যাক্টিভেট করুন:**
   - **Windows:**

     ```bash
     .\venv\Scripts\activate
     ```

   - **Mac/Linux:**

     ```bash
     source venv/bin/activate
     ```

4. **প্রয়োজনীয় লাইব্রেরিগুলো ইনস্টল করুন:**

   ```bash
   pip install fastapi uvicorn pypdf chromadb google-generativeai langchain langchain-google-genai
   ```

   *(অথবা যদি `requirements.txt` থাকে তবে: `pip install -r ../../requirment.txt`)*

5. **ব্যাকএন্ড সার্ভার রান করুন:**

   ```bash
   uvicorn main:app --reload
   ```

   ব্যাকএন্ড রান হলে আপনি এটি `http://127.0.0.1:8000` এ দেখতে পাবেন।

---

## 2️⃣ ফ্রন্টএন্ড সেটআপ (React Frontend)

ফ্রন্টএন্ড রান করার জন্য একটি নতুন টার্মিনাল খুলুন এবং নিচের কমান্ডগুলো দিন:

1. **ফ্রন্টএন্ড ফোল্ডারে যান:**

   ```bash
   cd FrontEnd
   ```

2. **ডিপেন্ডেন্সিগুলো ইনস্টল করুন:**

   ```bash
   npm install
   ```

3. **ডেভেলপমেন্ট সার্ভার রান করুন:**

   ```bash
   npm run dev
   ```

   ফ্রন্টএন্ড রান হলে আপনি এটি `http://localhost:5173` লিঙ্কে ওপেন করতে পারবেন।

---

## 📝 কিছু গুরুত্বপূর্ণ তথ্য

- **Database:** প্রোজেক্টটি প্রথমবার রান করার সময় অটোমেটিক `chloris.db` ডাটাবেস তৈরি করে নেবে।
- **CORS:** ফ্রন্টএন্ড এবং ব্যাকএন্ড যাতে একে অপরের সাথে কথা বলতে পারে, তার জন্য `main.py` ফাইলে CORS কনফিগার করা আছে।
- **Login:** আপনার ডাটাবেসে যদি স্টুডেন্ট ডাটা না থাকে, তবে `seed_student_data.py` ফাইলটি রান করে ডামি ডাটা ইনসার্ট করে নিতে পারেন।

শুভ গেমিং (বা কোডিং)! 🚀
